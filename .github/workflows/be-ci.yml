name: Backend CI

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'apps/be/**'
      - '.github/workflows/be-ci.yml'
  pull_request:
    branches:
      - main
      - master
    paths:
      - 'apps/be/**'
      - '.github/workflows/be-ci.yml'

jobs:
  lint:
    name: Lint Backend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/be
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: 'apps/be/go.mod'
          check-latest: true
          cache-dependency-path: apps/be/go.sum

      - name: Cache golangci-lint Binary
        uses: actions/cache@v4
        id: cache-golangci-lint
        with:
          path: ~/go/bin/golangci-lint
          key: ${{ runner.os }}-golangci-lint-v1.64

      - name: Install golangci-lint
        if: steps.cache-golangci-lint.outputs.cache-hit != 'true'
        run: go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest

      - name: Run golangci-lint
        run: |
          export PATH=$PATH:$(go env GOPATH)/bin
          golangci-lint run ./... --timeout=5m

  test:
    name: Test & Build Backend
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgrespassword
          POSTGRES_DB: ticketbox_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    defaults:
      run:
        working-directory: apps/be
    env:
      DB_HOST: localhost
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: postgrespassword
      DB_NAME: ticketbox_test
      DB_SSLMODE: disable

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: 'apps/be/go.mod'
          check-latest: true
          cache-dependency-path: apps/be/go.sum

      - name: Verify Dependencies
        run: go mod verify

      - name: Run Vet Analysis
        run: go vet ./...

      - name: Run Tests (Unit & PostgreSQL Integration)
        run: go test -v -race -coverprofile=coverage.out ./internal/...

      - name: Build Binary
        run: go build -v -o bin/api ./cmd/api/main.go
